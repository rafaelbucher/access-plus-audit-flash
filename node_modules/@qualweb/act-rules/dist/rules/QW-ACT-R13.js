"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.QW_ACT_R13 = void 0;
const applicability_1 = require("@qualweb/util/applicability");
const evaluation_1 = require("@qualweb/core/evaluation");
const AtomicRule_object_1 = require("../lib/AtomicRule.object");
class QW_ACT_R13 extends AtomicRule_object_1.AtomicRule {
    execute(element) {
        const test = new evaluation_1.Test();
        const children = element.getElementChildren();
        if (children.length > 0) {
            const focusable = this.isFocusableChildren(element);
            if (focusable) {
                test.verdict = evaluation_1.Verdict.FAILED;
                test.resultCode = 'F1';
            }
            else {
                test.verdict = evaluation_1.Verdict.PASSED;
                test.resultCode = 'P1';
            }
        }
        else {
            const focusable = window.AccessibilityUtils.isPartOfSequentialFocusNavigation(element);
            if (focusable) {
                test.verdict = evaluation_1.Verdict.FAILED;
                test.resultCode = 'F2';
            }
            else {
                test.verdict = evaluation_1.Verdict.PASSED;
                test.resultCode = 'P2';
            }
        }
        test.addElement(element);
        this.addTestResult(test);
    }
    isFocusableChildren(element) {
        let result = window.AccessibilityUtils.isPartOfSequentialFocusNavigation(element);
        const children = element.getElementChildren();
        for (const child of children || []) {
            const focusable = window.AccessibilityUtils.isPartOfSequentialFocusNavigation(child);
            if (focusable) {
                this.triggerFocus(child);
                const newFocusedElement = window.qwPage.getFocusedElement();
                if (child.getElementSelector() === (newFocusedElement === null || newFocusedElement === void 0 ? void 0 : newFocusedElement.getElementSelector())) {
                    result = true;
                }
            }
            else {
                const childFocusable = this.isFocusableChildren(child);
                result = result || childFocusable;
            }
        }
        return result;
    }
    triggerFocus(element) {
        const event = new Event('focus', { bubbles: false, cancelable: true });
        element.focusElement();
        element.dispatchEvent(event);
    }
}
exports.QW_ACT_R13 = QW_ACT_R13;
__decorate([
    applicability_1.ElementExists,
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [Function]),
    __metadata("design:returntype", void 0)
], QW_ACT_R13.prototype, "execute", null);
