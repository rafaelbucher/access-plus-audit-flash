import type { EvaluationReport, SystemData, QualwebReport, CounterReport } from './evaluation';
export declare class Report {
    private readonly systemData;
    private readonly metadata;
    private readonly modules;
    constructor(systemData: SystemData);
    addModuleReport(moduleReport: EvaluationReport | CounterReport): void;
    getCopy(): QualwebReport;
}
//# sourceMappingURL=Report.object.d.ts.map